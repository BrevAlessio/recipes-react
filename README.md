# Instruction

```
npm install
npm run dev
```

- Small focused components.
- Mostly local state, except context used once.
- Keep the UI simple usable with a keyboard.
- Everything on the same page, so no routing.
- Some BEM structure and CSS variables for styling.
